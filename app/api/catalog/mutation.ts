export async function forwardCatalogMutation(request:Request, context:{params:Promise<Record<string,string>>}, method:string, kind:string){const backendUrl=process.env.DEVELOCRM_API_URL?.replace(/\/$/,"");const tenantId=process.env.DEVELOCRM_TENANT_ID;const authorization=request.headers.get("authorization");if(!backendUrl||!tenantId||!authorization)return Response.json({error:"Editace vyžaduje připojený backend"},{status:503});const p=await context.params;const target=kind==="project"?`/v1/projects/${p.projectId}`:kind==="unit"?`/v1/units/${p.unitId}`:kind==="unit-accessory"?`/v1/units/${p.unitId}/accessories`:`/v1/accessory-assignments/${p.assignmentId}`;const response=await fetch(`${backendUrl}${target}`,{method,headers:{authorization,"x-tenant-id":tenantId,"content-type":"application/json"},body:method==="DELETE"?undefined:await request.text()});return new Response(await response.text(),{status:response.status,headers:{"content-type":"application/json"}});}
