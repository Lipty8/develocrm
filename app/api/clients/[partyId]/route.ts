export async function PATCH(request:Request,context:{params:Promise<{partyId:string}>}){return forward(request,context,"PATCH",false);}
export async function POST(request:Request,context:{params:Promise<{partyId:string}>}){return forward(request,context,"POST",true);}
async function forward(request:Request,context:{params:Promise<{partyId:string}>},method:string,contact:boolean){const base=process.env.DEVELOCRM_API_URL?.replace(/\/$/,"");const tenant=process.env.DEVELOCRM_TENANT_ID;const auth=request.headers.get("authorization");if(!base||!tenant||!auth)return Response.json({error:"Editace vyžaduje připojený backend"},{status:503});const {partyId}=await context.params;const target=contact?`/v1/parties/${partyId}/contacts`:`/v1/parties/${partyId}`;const res=await fetch(base+target,{method,headers:{authorization:auth,"x-tenant-id":tenant,"content-type":"application/json"},body:await request.text()});return new Response(await res.text(),{status:res.status,headers:{"content-type":"application/json"}});}
